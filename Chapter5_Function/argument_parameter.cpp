#include <bits/stdc++.h>
using namespace std;

void greet(string name) {       // name is a parameter
    cout << "Hello, " << name << endl;
}

int main() {

    string name = "Rana";
    // cin >> name;
    greet(name);                // name is a argument

  return 0;
}